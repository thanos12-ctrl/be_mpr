const dummyTopics: Record<string, Topic> = {
  '1': {
    id: '1',
    bundleId: '1',
    title: 'Java Comments',
    introduction: `Welcome to the world of Java programming! As you start writing code, you'll quickly realize that programs aren't just for computers to run; they're also for humans to read and understand. This is where comments come in. Comments are notes you can add to your code that the Java compiler completely ignores. They are crucial for explaining your code's purpose, making it easier for yourself and others to understand later on.`,
    mainContent: "Java provides three types of comments:\n\n1.  **Single-line Comments (`//`):** These are the most common type. Anything you type after `//` on the same line is considered a comment. They are great for quick explanations or to temporarily disable a line of code.\n    *   Example: `// This is a single-line comment`\n\n2.  **Multi-line Comments (`/* ... */`):** Also known as block comments, these allow you to write comments that span multiple lines. They start with `/*` and end with `*/`. Everything between these two markers is ignored by the compiler. These are useful for more extensive explanations or for commenting out larger blocks of code.\n    *   Example:\n        java\n        /*\n         * This is a multi-line comment.\n         * It can span across several lines.\n         */\n        \n\n3.  **Documentation Comments (`/** ... */`):** These are a special type of multi-line comment used for generating documentation (using the `javadoc` tool). They start with `/**` and end with `*/`. While functionally similar to multi-line comments for the compiler, they are semantically different as they are processed by the `javadoc` utility to create API documentation. For now, focus primarily on single-line and multi-line comments, but be aware of this third type.\n\nRemember, comments do not affect how your program runs; they are purely for documentation and readability.",
    codeExample: `public class CommentExamples {\n\n    public static void main(String[] args) {\n        // This is a single-line comment.\n        // It explains the next line of code.\n        System.out.println(\"Hello, Java!\"); // This prints \"Hello, Java!\" to the console.\n\n        /*\n         * This is a multi-line comment.\n         * It provides a more detailed explanation\n         * about the section of code below.\n         */\n        int number = 10; // Declare and initialize an integer variable\n        System.out.println(\"The number is: \" + number);\n\n        /**\n         * This is a documentation comment.\n         * It's often used for classes, methods, and variables\n         * to generate API documentation.\n         * For beginners, single-line and multi-line comments are more common.\n         */\n        String message = \"Learning Java!\";\n        System.out.println(message);\n    }\n}`,
    keyPoints: [
      "Comments are notes in code ignored by the Java compiler.",
      "They improve code readability and understanding for humans.",
      "Single-line comments start with `//`.",
      "Multi-line comments start with `/*` and end with `*/`.",
      "Documentation comments (`/** ... */`) are a special type for generating API documentation."
    ],
    estimatedTime: 15,
  },
  '2': {

    id: '2',
    bundleId: '2',
    title: 'Java variable',
    introduction: "Imagine you're baking and need to store different ingredients, like flour, sugar, or eggs. In programming, variables act like these containers, holding different pieces of information that your program will use. Learning about variables is a foundational step, as they are essential for storing and manipulating data in any Java application.",
    mainContent: "In Java, a variable is a named memory location used to store data. Each variable has a specific 'type' which determines what kind of data it can hold (e.g., numbers, text, true/false values). Once declared, a variable can be assigned a value, and its value can change during the program's execution.\n\nThere are three main steps when working with variables:\n\n1.  **Declaration:** This is where you tell Java the name of your variable and what type of data it will store. The syntax is `dataType variableName;`\n    *   Example: `int age;` (declares a variable named `age` that will hold an integer).\n\n2.  **Initialization:** This is the act of giving a variable its very first value. You can initialize a variable at the same time you declare it, or later.\n    *   Example (Declaration and Initialization): `int age = 30;`\n\n3.  **Assignment:** After initialization, you can change the value of a variable at any point in your program. You simply use the assignment operator (`=`).\n    *   Example: `age = 31;`\n\n**Important Data Types (for now):**\n*   `int`: For whole numbers (e.g., 10, -500).\n*   `double`: For floating-point numbers (numbers with decimal points, e.g., 3.14, -0.5).\n*   `boolean`: For true/false values (e.g., `true`, `false`).\n*   `String`: For text (e.g., \"Hello World\", \"Java\"). Note: `String` starts with a capital 'S' because it's a class, not a primitive type, but acts like one for basic usage.\n\nVariable names should be descriptive and follow Java's naming conventions (e.g., `camelCase` for variable names, starting with a lowercase letter).",
    codeExample: "public class VariableExample {\n    public static void main(String[] args) {\n        // 1. Declaring an integer variable 'score'\n        int score;\n\n        // 2. Initializing the 'score' variable with a value\n        score = 100;\n        System.out.println(\"Initial Score: \" + score); // Output: Initial Score: 100\n\n        // 3. Declaring and initializing a String variable 'userName' in one line\n        String userName = \"Alice\";\n        System.out.println(\"User Name: \" + userName); // Output: User Name: Alice\n\n        // 4. Declaring and initializing a double variable 'price'\n        double price = 19.99;\n        System.out.println(\"Product Price: \" + price); // Output: Product Price: 19.99\n\n        // 5. Declaring a boolean variable 'isLoggedIn'\n        boolean isLoggedIn;\n        // 6. Initializing 'isLoggedIn'\n        isLoggedIn = true;\n        System.out.println(\"Is User Logged In?: \" + isLoggedIn); // Output: Is User Logged In?: true\n\n        // 7. Re-assigning a new value to the 'score' variable\n        score = 120;\n        System.out.println(\"Updated Score: \" + score); // Output: Updated Score: 120\n\n        // 8. Re-assigning a new value to the 'userName' variable\n        userName = \"Bob\";\n        System.out.println(\"New User Name: \" + userName); // Output: New User Name: Bob\n    }\n}\n",
    keyPoints: [
      "Variables are named memory locations used to store data in a Java program.",
      "Every variable must have a specific data type (e.g., `int`, `double`, `boolean`, `String`) that dictates the kind of data it can hold.",
      "Variables can be declared (`dataType variableName;`), initialized (given an initial value), and their values can be reassigned during program execution.",
      "Good variable names are descriptive and follow `camelCase` conventions (e.g., `firstName`, `itemCount`)."
    ],
    estimatedTime: 15,
  },
};